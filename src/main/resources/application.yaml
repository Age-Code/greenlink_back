spring:
  datasource:
    url: jdbc:mysql://localhost:3306/greenlink?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: age0504!
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  sql:
    init:
      mode: never

jwt:
  secret: greenlink-jwt-secret-key-greenlink-jwt-secret-key-2026
  access-token-validity-ms: 86400000